# Web-counter
a simple containerized Flask web application integrated with Redis for persistent visitor tracking. The application demonstrates Docker-based development, service communication, and live development workflows using Docker Compose.

services:
  web:
    build: .
    ports:
      - "8080:5000"
    volumes:
      - .:/app
    environment:
      FLASK_DEBUG: "1"
    command: flask run --host=0.0.0.0 --port=5000
  redis:
    image: redis:alpine

  